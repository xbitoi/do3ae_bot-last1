{
  "name": "do3aelasto-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run build --workspaces --if-present && cd artifacts/api-server && npm run start",
    "build": "pip3 install --user arabic-reshaper python-bidi pillow || true && npm run build --workspaces --if-present",
    "start": "cd artifacts/api-server && npm run start"
  },
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "dependencies": {
    "@huggingface/hub": "^2.13.0",
    "playwright": "^1.60.0"
  }
}
